# Server
fastapi==0.115.0
uvicorn==0.30.6
websockets==12.0
python-multipart==0.0.9
pydantic==2.8.2

# Audio processing
numpy==1.26.4
scipy==1.13.1
soundfile==0.12.1

# VAD - Silero ONNX
onnxruntime==1.18.1

# Classifier - Whisper via faster-whisper (CTranslate2, 4x mas rapido que openai-whisper)
faster-whisper==1.0.3

# Cascade AMD v1 (state machine de 4 etapas)
webrtcvad-wheels==2.0.14   # VAD binario, wheels precompilados (sin compilar C)
aubio==0.4.9               # YIN pitch detection (F0)
